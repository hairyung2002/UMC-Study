import React from "react";
import { useParams } from "react-router-dom";

const MovieDetailPage = (): Element => {
    const params = useParams();

    return <div>MovieDetalPage</div>;
}

export default MovieDetailPage;